#include	"CarbonXCmd.h"#include	<cstring>#include	<quickdraw.h>pascal void	main( XCmdBlockPtr theBlock ){	Str255			vCountString;	long			vCount,					x;	unsigned long	dummy;	CGrafPtr		vPort;	Rect			vBox;		GetPort( &vPort );	GetPortBounds( vPort, &vBox );		ZeroToPas( theBlock, XCMD_GetHandleChars( theBlock, theBlock->params[0] ), vCountString );	vCount = StrToLong( theBlock, vCountString );	for( x = 1; x <= vCount; x++ )	{		InvertRect( &vBox );		Delay( 3, &dummy );		InvertRect( &vBox );		Delay( 3, &dummy );	}}